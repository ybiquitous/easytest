name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  test:
    uses: ybiquitous/.github/.github/workflows/ruby-test-reusable.yml@6449c410d1b8ad8f7f6ac9e270441321ee65a5f6 # v3.0.1
    with:
      minimum-ruby-version: 3.2 # sync with `required_ruby_version` in gemspec
    permissions:
      contents: read
