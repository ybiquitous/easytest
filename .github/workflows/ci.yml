name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  test:
    uses: ybiquitous/.github/.github/workflows/ruby-test-reusable.yml@main
    with:
      ruby-version: '["2.7", "3.0", "3.1", "3.2"]'
