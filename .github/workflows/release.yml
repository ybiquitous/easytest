name: Release

on:
  workflow_dispatch:

jobs:
  release:
    # TODO: Try the new release workflow on main. Once it is verified, switch to a published version.
    uses: ybiquitous/.github/.github/workflows/ruby-release-reusable.yml@63562a3e2d31bcd83e4cdb2cee35875edbba2ad6 # main
    permissions:
      contents: write
      id-token: write
